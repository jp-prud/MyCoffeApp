import {Screen, Text} from '@components';

export function NotificationScreen() {
  return (
    <Screen canGoBack scrollable>
      <Text preset="headingLarge">Notification</Text>
    </Screen>
  );
}
